@model IEnumerable<SCS.Models.OrderAccessories>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Accessories.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order.StatusOrder)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rates.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Accessories.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order.StatusOrder)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rates.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
